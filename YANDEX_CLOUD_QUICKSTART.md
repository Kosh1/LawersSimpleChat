# 🚀 Быстрый старт: Деплой на Yandex Cloud

## ✅ Что уже готово

Все файлы для деплоя уже созданы:
- ✅ `next.config.mjs` - настроен для Docker
- ✅ `Dockerfile` - готов к использованию
- ✅ `.dockerignore` - настроен
- ✅ GitHub Actions workflow - готов к работе

## 📝 Что нужно сделать

### 1. Настройка Yandex Cloud (3 минуты)

1. **Создайте Container Registry** (если еще нет)
   - Console → Container Registry → Создать реестр
   - Запомните **ID реестра** (Registry ID)

2. **Сервисный аккаунт `deploy-bot`** ✅
   - У вас уже есть сервисный аккаунт `deploy-bot` с ролью `editor`
   - Роль `editor` дает все необходимые права для деплоя
   - У вас уже есть **статический ключ доступа** для этого аккаунта ✅
   - Если нужно создать новый статический ключ:
     - Console → Service Accounts → `deploy-bot` → Авторизованные ключи → Создать статический ключ доступа
     - **Сохраните Access Key ID и Secret Access Key**

3. **Создайте Serverless Container**
   - Console → Serverless Containers → Создать
   - Имя: `jhelper-bot`

4. **Узнайте ID каталога и облака**
   - Console → вверху страницы будет **Cloud ID** и **Folder ID**
   - Запомните оба значения

### 2. Настройка GitHub Secrets (3 минуты)

GitHub → Settings → Secrets and variables → Actions → New repository secret

**Обязательные:**
- `YC_ACCESS_KEY_ID` - Access Key ID из статического ключа `deploy-bot`
- `YC_SECRET_ACCESS_KEY` - Secret Access Key из статического ключа `deploy-bot`
- `YC_CLOUD_ID` - ID облака (вверху страницы консоли)
- `YC_FOLDER_ID` - ID каталога (вверху страницы консоли)
- `YC_REGISTRY_ID` - ID реестра (например, `crp1234567890abcdef`)
- `YC_SERVICE_ACCOUNT_ID` - ID сервисного аккаунта `deploy-bot` (например, `aje1234567890abcdef`)
  - Найти: Console → Service Accounts → `deploy-bot` → ID вверху страницы

**Переменные окружения:**
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `NEXT_PUBLIC_SITE_URL`
- `OPENROUTER_API_KEY`
- `OPENAI_API_KEY`
- `NEXT_PUBLIC_PROXY_URL` (опционально)
- `NEXT_PUBLIC_ENABLE_SIGNUP` (опционально)

### 3. Деплой (автоматически)

```bash
git add .
git commit -m "Add Yandex Cloud deployment"
git push origin main
```

GitHub Actions автоматически:
1. Соберет проект
2. Создаст Docker образ
3. Загрузит в Container Registry
4. Задеплоит в Serverless Container

**Время деплоя:** 5-10 минут

### 4. Получение URL

После успешного деплоя:
- Yandex Cloud Console → Serverless Containers → `jhelper-bot`
- URL будет в формате: `https://jhelper-bot-xxxxx.containers.yandexcloud.net`

## 📚 Подробная инструкция

См. [docs/YANDEX_CLOUD_DEPLOY.md](docs/YANDEX_CLOUD_DEPLOY.md)

## 🐛 Проблемы?

1. Проверьте логи в GitHub Actions
2. Убедитесь, что все секреты добавлены
3. Проверьте роли Service Account
4. См. раздел Troubleshooting в подробной инструкции

## 💰 Стоимость

Примерно **500-2000₽/месяц** при небольшой нагрузке

---

**Готово!** После настройки каждый push в `main` будет автоматически деплоить приложение 🎉

