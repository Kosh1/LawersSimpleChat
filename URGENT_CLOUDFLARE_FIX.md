# 🚨 СРОЧНО: Исправьте настройки в Cloudflare Dashboard

## ❌ Проблема

В вашем проекте Cloudflare Pages установлены **неправильные настройки**:
- Build command: `npm run build` ❌ (должно быть с `@cloudflare/next-on-pages`)
- Deploy command: `npx wrangler deploy` ❌ (это команда для Workers, не для Pages)

## ✅ РЕШЕНИЕ (выполните СЕЙЧАС)

### Шаг 1: Откройте Cloudflare Dashboard
1. Перейдите на https://dash.cloudflare.com/
2. Войдите в свой аккаунт
3. Слева найдите **Workers & Pages**
4. Выберите ваш проект Pages

### Шаг 2: Измените Build Command
1. Откройте вкладку **Settings** (вверху страницы)
2. Прокрутите до раздела **Builds & deployments**
3. Найдите поле **"Build command"**
4. **Замените** `npm run build` на:
   ```
   npm run build && npx @cloudflare/next-on-pages@latest
   ```
5. Нажмите кнопку **Save**

### Шаг 3: Удалите Deploy Command
1. В том же разделе **Builds & deployments**
2. Найдите поле **"Deploy command"** (может называться "Post-build command")
3. **УДАЛИТЕ** полностью текст: `npx wrangler deploy`
4. **Оставьте поле ПУСТЫМ**
5. Нажмите кнопку **Save**

### Шаг 4: Проверьте Build Output Directory
Убедитесь, что в поле **"Build output directory"** указано:
```
.vercel/output/static
```

## ⚠️ ВАЖНО

- Эти настройки **НЕЛЬЗЯ** изменить через код в репозитории
- Это нужно сделать **ВРУЧНУЮ** в веб-интерфейсе Cloudflare
- После сохранения Cloudflare автоматически запустит новый деплой

## ✅ После исправления

Настройки должны быть такими:

```
Build command: npm run build && npx @cloudflare/next-on-pages@latest
Build output directory: .vercel/output/static
Deploy command: (пусто - удалено)
```

## 📞 Если не получается

Если вы не можете найти эти настройки:
1. Убедитесь, что вы редактируете проект типа **Pages**, а не **Workers**
2. Настройки находятся в **Settings** → **Builds & deployments**
3. Если все еще не видите - попробуйте обновить страницу

**Без этих изменений деплой НИКОГДА не пройдет успешно!**

