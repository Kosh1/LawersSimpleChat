# 🔧 Настройка AI моделей

## ⚠️ ВАЖНО: Выберите правильную конфигурацию!

Система поддерживает несколько вариантов конфигурации в зависимости от того, к каким моделям OpenAI у вас есть доступ.

---

## 📋 Какие модели у вас доступны?

### Проверьте доступ в OpenAI Playground:

1. Перейдите на https://platform.openai.com/playground
2. Откройте dropdown со списком моделей
3. Посмотрите какие модели доступны

**Возможные варианты:**

- ✅ **GPT-5** (`gpt-5`, `gpt-5-mini`, `gpt-5-nano`) - новейшие модели
- ✅ **O1 Reasoning** (`o1-preview`, `o1-mini`) - модели с глубоким reasoning
- ✅ **GPT-4o** (`gpt-4o`, `gpt-4o-mini`) - текущие стабильные модели
- ✅ **GPT-4 Turbo** (`gpt-4-turbo`, `gpt-4-turbo-2024-04-09`) - проверенные модели

---

## 🔄 Как настроить конфигурацию

### Шаг 1: Откройте файл конфигурации

```bash
lib/model-config.ts
```

### Шаг 2: Найдите раздел с конфигурациями

В файле есть 3 варианта конфигурации:

```typescript
// ВАРИАНТ 1: GPT-5 MODELS (если доступны)
export const MODEL_CONFIGS = { ... }

// ВАРИАНТ 2: O1 REASONING MODELS
// export const MODEL_CONFIGS = { ... }

// ВАРИАНТ 3: GPT-4O MODELS
// export const MODEL_CONFIGS = { ... }
```

### Шаг 3: Выберите нужный вариант

**Закомментируйте текущую конфигурацию** (оберните в `/* ... */`) и **раскомментируйте** нужную.

---

## 📝 Варианты конфигурации

### ВАРИАНТ 1: GPT-5 (если доступна)

**Когда использовать:** У вас есть доступ к GPT-5 API

**Преимущества:**
- ✅ Максимальный контекст (400K токенов)
- ✅ Максимальный ответ (32K токенов)
- ✅ Reasoning параметры (reasoning_effort, verbosity)
- ✅ Идеально для длинных документов

**Модели:**
- `primary`: `gpt-5` или `gpt-5-chat-latest`
- `reasoning`: `gpt-5` или `gpt-5-mini`
- `fallback`: `gpt-4-turbo`

**Как настроить:**
```typescript
// В lib/model-config.ts оставьте этот блок активным (раскомментированным)
export const MODEL_CONFIGS: Record<ModelName, ModelConfig> = {
  'primary': {
    name: 'gpt-5', // ← измените если нужно
    maxTokens: 32000,
    contextWindow: 400000,
    // ...
  },
  // ...
};
```

---

### ВАРИАНТ 2: O1 Reasoning Models

**Когда использовать:** У вас есть доступ к O1 (o1-preview, o1-mini)

**Преимущества:**
- ✅ Глубокий reasoning для сложных задач
- ✅ Отлично для юридического анализа
- ✅ Хорошо работает с большими контекстами

**Особенности O1:**
- ❌ **НЕ поддерживает** `temperature`
- ❌ **НЕ поддерживает** `system` messages (используется `developer`)
- ✅ Использует `max_completion_tokens` вместо `max_tokens`

**Модели:**
- `primary`: `gpt-4o` (быстрая основная)
- `reasoning`: `o1-preview` (глубокий анализ)
- `fallback`: `gpt-4-turbo`

**Как настроить:**
```typescript
// В lib/model-config.ts:

// 1. Закомментируйте ВАРИАНТ 1:
/*
export const MODEL_CONFIGS: Record<ModelName, ModelConfig> = {
  // ... вариант 1
};
*/

// 2. Раскомментируйте ВАРИАНТ 2:
export const MODEL_CONFIGS: Record<ModelName, ModelConfig> = {
  'primary': {
    name: 'gpt-4o', // ← основная быстрая модель
    // ...
  },
  'reasoning': {
    name: 'o1-preview', // ← O1 для сложных задач
    temperature: undefined, // ← O1 не поддерживает!
    useMaxCompletionTokens: true, // ← важно!
    supportsSystemMessages: false, // ← важно!
    useDeveloperMessage: true, // ← важно!
    // ...
  },
  // ...
};
```

---

### ВАРИАНТ 3: GPT-4o Models (стабильные)

**Когда использовать:** 
- У вас нет доступа к GPT-5 или O1
- Нужна проверенная стабильная конфигурация
- Бюджетное решение

**Преимущества:**
- ✅ Доступны всем пользователям OpenAI API
- ✅ Стабильные и надежные
- ✅ Хорошее качество ответов
- ✅ Разумная цена

**Модели:**
- `primary`: `gpt-4o` или `gpt-4o-2024-08-06`
- `reasoning`: `gpt-4o` (та же модель, детальные промпты)
- `fallback`: `gpt-4-turbo`

**Как настроить:**
```typescript
// В lib/model-config.ts:

// 1. Закомментируйте ВАРИАНТ 1:
/*
export const MODEL_CONFIGS: Record<ModelName, ModelConfig> = {
  // ... вариант 1
};
*/

// 2. Раскомментируйте ВАРИАНТ 3:
export const MODEL_CONFIGS: Record<ModelName, ModelConfig> = {
  'primary': {
    name: 'gpt-4o', // ← основная модель
    maxTokens: 16000,
    contextWindow: 128000,
    // ...
  },
  'reasoning': {
    name: 'gpt-4o', // ← та же модель для reasoning
    // ...
  },
  // ...
};
```

---

## 🎯 Рекомендации по выбору

### Если у вас есть доступ к GPT-5:
✅ **Используйте ВАРИАНТ 1**
- Лучшее качество
- Максимальные лимиты
- Идеально для длинных документов

### Если у вас есть доступ к O1:
✅ **Используйте ВАРИАНТ 2**
- Отлично для юридических задач
- Глубокий reasoning
- Хорошее качество анализа

### Если доступны только GPT-4o/GPT-4:
✅ **Используйте ВАРИАНТ 3**
- Стабильное решение
- Проверенное временем
- Доступно всем

---

## 🔍 Проверка конфигурации

### После настройки:

1. **Перезапустите сервер:**
```bash
npm run dev
```

2. **Откройте консоль браузера** (F12)

3. **Сделайте запрос**

4. **Проверьте логи:**
```javascript
[AI Service] Primary model selected: primary
[AI Service] Attempting model: primary (1/3)
[AI Service] Success with model: primary
AI Response metadata: {
  modelUsed: 'primary', // ← должно быть 'primary' или 'reasoning'
  // ...
}
```

---

## 🐛 Troubleshooting

### Ошибка: "The model `gpt-5` does not exist"

**Проблема:** У вас нет доступа к GPT-5

**Решение:** 
1. Переключитесь на ВАРИАНТ 2 (O1) или ВАРИАНТ 3 (GPT-4o)
2. Проверьте доступ в OpenAI Playground

---

### Ошибка: "Invalid 'messages[0].role'"

**Проблема:** O1 не поддерживает `system` messages

**Решение:**
1. Убедитесь что используете ВАРИАНТ 2
2. Проверьте что `supportsSystemMessages: false`
3. Проверьте что `useDeveloperMessage: true`
4. Система автоматически конвертирует `system` → `developer`

---

### Ошибка: "Invalid parameter: 'temperature'"

**Проблема:** O1 не поддерживает temperature

**Решение:**
1. В ВАРИАНТЕ 2 убедитесь что `temperature: undefined`
2. Система автоматически НЕ будет передавать temperature для O1

---

### Ошибка: "Invalid parameter: 'max_tokens'"

**Проблема:** O1 использует `max_completion_tokens`

**Решение:**
1. В ВАРИАНТЕ 2 убедитесь что `useMaxCompletionTokens: true`
2. Система автоматически использует правильный параметр

---

## 📚 Дополнительные ресурсы

### Официальная документация OpenAI:

- **Models:** https://platform.openai.com/docs/models
- **Chat Completions:** https://platform.openai.com/docs/api-reference/chat
- **O1 Models:** https://platform.openai.com/docs/guides/reasoning
- **Pricing:** https://openai.com/pricing

### Проверка доступа:

- **API Keys:** https://platform.openai.com/api-keys
- **Playground:** https://platform.openai.com/playground
- **Usage:** https://platform.openai.com/usage

---

## ✅ Checklist после настройки

- [ ] Выбрал нужный ВАРИАНТ конфигурации
- [ ] Закомментировал неиспользуемые варианты
- [ ] Проверил имена моделей в Playground
- [ ] Обновил имена моделей в конфигурации
- [ ] Перезапустил сервер
- [ ] Протестировал простой запрос
- [ ] Протестировал сложный запрос
- [ ] Проверил логи в консоли
- [ ] Все работает! 🎉

---

## 💡 Советы

1. **Начните с ВАРИАНТА 3** (GPT-4o) если не уверены
2. **Постепенно переходите** на более мощные модели
3. **Мониторьте costs** в OpenAI dashboard
4. **Тестируйте** перед использованием в продакшене
5. **Сохраните backup** рабочей конфигурации

---

## 📞 Нужна помощь?

1. Проверьте логи в консоли браузера (F12)
2. Проверьте логи сервера (терминал)
3. Посмотрите `AI_MODELS_GUIDE.md`
4. Проверьте официальную документацию OpenAI

---

## 🎉 Готово!

После настройки система будет автоматически:
- ✅ Выбирать лучшую модель для задачи
- ✅ Делать fallback при ошибках
- ✅ Генерировать длинные ответы через chunking
- ✅ Адаптировать параметры для каждой модели
- ✅ Конвертировать messages для O1 (если используется)

**Удачи!** 🚀

