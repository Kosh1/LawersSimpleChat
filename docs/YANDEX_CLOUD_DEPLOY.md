# 🚀 Деплой на Yandex Cloud Serverless Containers

Пошаговая инструкция по настройке автоматического деплоя Next.js приложения на Yandex Cloud.

## ✅ Что уже настроено в проекте

- ✅ `next.config.mjs` - добавлен `output: 'standalone'` для Docker
- ✅ `Dockerfile` - многоэтапная сборка для оптимизации
- ✅ `.dockerignore` - исключение ненужных файлов
- ✅ GitHub Actions workflow - автоматический деплой

## 📋 Шаг 1: Настройка Yandex Cloud

### 1.1. Создание каталога

1. Зайдите на https://console.cloud.yandex.ru/
2. Если каталога нет, создайте его
3. Запомните **ID каталога** (Folder ID) - понадобится позже

### 1.2. Создание Container Registry

1. Перейдите в раздел **Container Registry**
2. Нажмите **Создать реестр**
3. Укажите имя (например, `jhelper-bot-registry`)
4. Выберите каталог
5. Нажмите **Создать**
6. **Запомните ID реестра** (Registry ID) - будет в формате `crp1234567890abcdef`

### 1.3. Использование существующего Service Account

У вас уже есть сервисный аккаунт `deploy-bot` с ролью `editor`.

**Роль `editor` дает все необходимые права:**
- ✅ `container-registry.images.pusher` - для загрузки образов
- ✅ `container-registry.images.puller` - для скачивания образов
- ✅ `serverless.containers.deployer` - для деплоя контейнеров
- ✅ `serverless.containers.invoker` - для вызова контейнеров

#### Использование статического ключа доступа:

У вас уже есть статический ключ доступа для `deploy-bot`. Если нужно создать новый:

1. Перейдите в **Service Accounts** → `deploy-bot`
2. Перейдите на вкладку **Авторизованные ключи**
3. Нажмите **Создать статический ключ доступа**
4. **СОХРАНИТЕ Access Key ID и Secret Access Key** - они понадобятся для GitHub Secrets

**Важно:** Статический ключ доступа - это AWS-совместимый ключ, который используется для аутентификации в Container Registry и для работы с yc CLI.

### 1.4. Создание Serverless Container

1. Перейдите в раздел **Serverless Containers**
2. Нажмите **Создать контейнер**
3. Укажите имя: `jhelper-bot`
4. Выберите каталог
5. Нажмите **Создать**
6. **Запомните имя контейнера** - `jhelper-bot`

## 📋 Шаг 2: Настройка GitHub Secrets

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**

### Обязательные секреты:

#### `YC_ACCESS_KEY_ID`
- **Значение**: Access Key ID из статического ключа доступа `deploy-bot`
- **Как получить**: Используйте Access Key ID из вашего статического ключа

#### `YC_SECRET_ACCESS_KEY`
- **Значение**: Secret Access Key из статического ключа доступа `deploy-bot`
- **Как получить**: Используйте Secret Access Key из вашего статического ключа

#### `YC_CLOUD_ID`
- **Значение**: ID облака Yandex Cloud
- **Как получить**: В консоли Yandex Cloud вверху страницы будет указан **Cloud ID**

#### `YC_FOLDER_ID`
- **Значение**: ID каталога (Folder ID)
- **Как получить**: В консоли Yandex Cloud вверху страницы будет указан **Folder ID**

#### `YC_REGISTRY_ID`
- **Значение**: ID реестра контейнеров (например, `crp1234567890abcdef`)
- **Как получить**: Container Registry → ваш реестр → ID вверху страницы

#### `YC_SERVICE_ACCOUNT_ID`
- **Значение**: ID сервисного аккаунта `deploy-bot` (например, `aje1234567890abcdef`)
- **Как получить**: Service Accounts → `deploy-bot` → ID вверху страницы

### Переменные окружения (из вашего проекта):

#### `NEXT_PUBLIC_SUPABASE_URL`
- URL вашего Supabase проекта

#### `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- Anon Key для Supabase

#### `NEXT_PUBLIC_SITE_URL`
- URL вашего сайта (после деплоя будет URL контейнера Yandex Cloud)

#### `OPENROUTER_API_KEY`
- API ключ OpenRouter (опционально)

#### `OPENAI_API_KEY`
- API ключ OpenAI (опционально)

#### `NEXT_PUBLIC_PROXY_URL`
- URL прокси-сервера (опционально)

#### `NEXT_PUBLIC_ENABLE_SIGNUP`
- `true` или `false` (опционально)

## 📋 Шаг 3: Первый деплой

1. Закоммитьте все изменения:
   ```bash
   git add .
   git commit -m "Add Yandex Cloud deployment configuration"
   git push origin main
   ```

2. GitHub Actions автоматически запустится
3. Проверьте статус в разделе **Actions** на GitHub
4. После успешного деплоя (5-10 минут) получите URL контейнера:
   - Yandex Cloud Console → Serverless Containers → `lawer-chat-bot`
   - URL будет в формате: `https://lawer-chat-bot-xxxxx.containers.yandexcloud.net`

## 📋 Шаг 4: Настройка домена (опционально)

1. В Yandex Cloud Console откройте **Serverless Containers** → `jhelper-bot`
2. Перейдите в раздел **Домены**
3. Нажмите **Добавить домен**
4. Укажите ваш домен
5. Настройте DNS-записи по инструкции в консоли

## 🔍 Проверка работы

1. Откройте URL контейнера в браузере
2. Проверьте работу основных страниц
3. Проверьте работу API routes (например, `/api/projects`)
4. Проверьте работу чата

URL контейнера:
- Yandex Cloud Console → Serverless Containers → `jhelper-bot`
- URL будет в формате: `https://jhelper-bot-xxxxx.containers.yandexcloud.net`

## 💰 Стоимость

Примерная стоимость при небольшой нагрузке:

- **Container Registry**: ~100₽/месяц за реестр
- **Serverless Container**: оплата за использование
  - Вычисления: ~0.001₽ за 100 мс
  - Память: ~0.01₽ за ГБ×час
  - Примерно **500-2000₽/месяц** при небольшой нагрузке

## 🐛 Troubleshooting

### Деплой не запускается

1. Проверьте, что все секреты добавлены в GitHub
2. Проверьте логи в GitHub Actions
3. Убедитесь, что Service Account имеет все необходимые роли

### Ошибка при сборке Docker образа

1. Проверьте, что `output: 'standalone'` добавлен в `next.config.mjs`
2. Проверьте логи сборки в GitHub Actions
3. Убедитесь, что все зависимости установлены

### Ошибка при деплое контейнера

1. Проверьте, что контейнер создан в Yandex Cloud
2. Проверьте, что Service Account имеет роль `serverless.containers.deployer`
3. Проверьте логи в Yandex Cloud Console

### Приложение не запускается

1. Проверьте переменные окружения в контейнере
2. Проверьте логи контейнера в Yandex Cloud Console
3. Убедитесь, что все необходимые переменные окружения добавлены

## 📚 Полезные ссылки

- [Документация Yandex Cloud Serverless Containers](https://cloud.yandex.ru/docs/serverless-containers/)
- [Документация Container Registry](https://cloud.yandex.ru/docs/container-registry/)
- [GitHub Actions для Yandex Cloud](https://github.com/marketplace/actions/yandex-cloud-setup)

## 🔄 Автоматический деплой

После настройки каждый push в ветку `main` или `master` будет автоматически запускать деплой на Yandex Cloud.

Процесс:
1. ✅ Установка зависимостей
2. ✅ Сборка Next.js приложения
3. ✅ Сборка Docker образа
4. ✅ Загрузка образа в Container Registry
5. ✅ Деплой в Serverless Container

Всё происходит автоматически! 🎉

