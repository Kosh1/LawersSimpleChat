# 🔐 Настройка Supabase Authentication

## Что Сделано

Реализована полноценная аутентификация через Supabase Auth вместо хранения userId в localStorage.

### Преимущества:
- ✅ Данные не теряются при очистке кэша
- ✅ Доступ с любого устройства/браузера  
- ✅ Безопасная аутентификация
- ✅ Настоящие пользовательские аккаунты
- ✅ Production-ready решение

---

## 🚀 Настройка Supabase Auth (ОБЯЗАТЕЛЬНО)

### Шаг 1: Включить Email Auth в Supabase

1. Откройте **Supabase Dashboard**
2. Перейдите в **Authentication** → **Providers**
3. Найдите **Email**
4. **Включите** Email provider (если еще не включен)
5. Настройте параметры:
   - ✅ Enable Email provider
   - ✅ Confirm email (опционально - можно отключить для тестирования)
   - ✅ Secure email change (рекомендуется)

### Шаг 2: Настроить Site URL

1. **Authentication** → **URL Configuration**
2. **Site URL**: `https://your-domain.com` (или `http://localhost:3000` для разработки)
3. **Redirect URLs**: Добавьте:
   ```
   http://localhost:3000/**
   https://your-domain.com/**
   ```

### Шаг 3: Настроить Email Templates (Опционально)

1. **Authentication** → **Email Templates**
2. Кастомизируйте письма:
   - Confirm signup
   - Magic Link
   - Reset password

---

## 📊 Миграция Существующих Данных

### Проблема

Старые проекты и чаты привязаны к случайным UUID из localStorage, которые не существуют в `auth.users`.

### Решение 1: Создать Тестового Пользователя и Привязать Данные

#### Шаг A: Создать первого пользователя

1. Запустите приложение: `npm run dev`
2. Откройте `http://localhost:3000/auth`
3. Зарегистрируйтесь с вашим email

#### Шаг B: Получить UUID нового пользователя

В Supabase SQL Editor:

```sql
-- Получить ID только что созданного пользователя
SELECT id, email FROM auth.users ORDER BY created_at DESC LIMIT 1;
```

Скопируйте `id` (это UUID).

#### Шаг C: Привязать старые данные к новому пользователю

```sql
-- Заменить YOUR_NEW_USER_ID на скопированный ID
UPDATE projects 
SET user_id = 'YOUR_NEW_USER_ID'
WHERE user_id IS NULL OR user_id NOT IN (SELECT id FROM auth.users);

-- Проверить
SELECT id, name, user_id FROM projects;
```

Теперь все старые проекты принадлежат вашему новому аккаунту!

---

### Решение 2: Очистить Старые Данные (Если не нужны)

**⚠️ ВНИМАНИЕ: Это удалит ВСЕ существующие проекты и чаты!**

```sql
-- Удалить все проекты (удалятся и чаты через cascade)
DELETE FROM projects WHERE user_id IS NULL 
OR user_id NOT IN (SELECT id FROM auth.users);

-- Удалить осиротевшие чаты
DELETE FROM chat_sessions WHERE user_id IS NULL 
OR user_id NOT IN (SELECT id FROM auth.users);
```

---

### Решение 3: Создать Пользователей для Существующих UUID (Сложно)

Если у вас много пользователей с разными UUID:

```sql
-- Посмотреть уникальные user_id
SELECT DISTINCT user_id FROM projects WHERE user_id IS NOT NULL;

-- Для каждого нужно либо:
-- 1. Создать реального пользователя вручную
-- 2. Или привязать к одному общему аккаунту (Решение 1)
```

---

## ✅ Проверка После Миграции

### 1. Проверить пользователей

```sql
SELECT id, email, created_at FROM auth.users;
```

### 2. Проверить проекты

```sql
SELECT 
  p.id, 
  p.name, 
  p.user_id,
  u.email
FROM projects p
LEFT JOIN auth.users u ON p.user_id = u.id
ORDER BY p.created_at DESC;
```

Все проекты должны иметь валидный `user_id` и соответствующий `email`.

### 3. Проверить чаты

```sql
SELECT 
  cs.id,
  cs.initial_message,
  cs.project_id,
  p.name as project_name,
  cs.user_id,
  u.email
FROM chat_sessions cs
LEFT JOIN projects p ON cs.project_id = p.id
LEFT JOIN auth.users u ON cs.user_id = u.id
ORDER BY cs.created_at DESC
LIMIT 10;
```

---

## 🧪 Тестирование

### 1. Регистрация

```bash
npm run dev
```

1. Откройте `http://localhost:3000/auth`
2. Нажмите "Нет аккаунта? Зарегистрироваться"
3. Введите email и пароль (мин. 6 символов)
4. Нажмите "Зарегистрироваться"

**Ожидаемый результат:**
- ✅ Сообщение "Регистрация успешна"
- ✅ Переход на страницу входа

### 2. Вход

1. Введите email и пароль
2. Нажмите "Войти"

**Ожидаемый результат:**
- ✅ Перенаправление на главную страницу
- ✅ Загружаются ваши проекты
- ✅ Можно создать новый проект

### 3. Проверка Persistence

1. Создайте проект "Test Auth"
2. Создайте чат, отправьте сообщение
3. Откройте DevTools (F12)
4. Application → Cookies → Удалите все cookies
5. Обновите страницу (F5)

**Ожидаемый результат:**
- ✅ Переход на `/auth` (разлогин)
- ✅ После повторного входа все проекты и чаты на месте!

### 4. Мультиустройство (Опционально)

1. Войдите с одного браузера
2. Откройте тот же URL в другом браузере
3. Войдите с теми же credentials

**Ожидаемый результат:**
- ✅ Видны те же проекты
- ✅ Те же чаты
- ✅ Синхронизация работает!

---

## 🔧 Troubleshooting

### Ошибка: "Unauthorized. Please sign in."

**Причина:** Auth middleware не может получить сессию

**Решение:**
1. Проверьте cookies разрешены в браузере
2. Убедитесь что домен правильный в Supabase settings
3. Попробуйте выйти и войти снова

### Ошибка: "User already registered"

**Решение:** Email уже используется, войдите вместо регистрации

### Ошибка: "Invalid login credentials"

**Решение:** Проверьте email и пароль, попробуйте сбросить пароль

### Проекты не загружаются после входа

**Причина:** Проекты привязаны к старым UUID

**Решение:** Выполните миграцию данных (см. выше)

---

## 📝 Изменения в Коде

### Новые Файлы:

- `lib/supabase/client.ts` - Клиент для browser
- `lib/supabase/server.ts` - Клиент для API routes
- `lib/supabase/middleware.ts` - Middleware helper
- `middleware.ts` - Next.js middleware для обновления сессий
- `lib/auth-helpers.ts` - Хелперы для проверки auth
- `hooks/use-auth.ts` - React hook для auth
- `components/auth-form.tsx` - UI для входа/регистрации
- `app/auth/page.tsx` - Страница авторизации

### Обновленные Файлы:

- `components/chat-page-client.tsx` - Использует useAuth
- `app/api/projects/route.ts` - Проверка auth
- `lib/supabase.ts` - Обертка для обратной совместимости

---

## 🎯 Следующие Шаги

1. ✅ Настроить Supabase Auth (Шаг 1-3)
2. ✅ Применить миграцию данных (если есть старые данные)
3. ✅ Протестировать регистрацию/вход
4. ✅ Протестировать persistence после очистки cookies
5. ✅ Задеплоить на production

---

## 🚀 Готово!

Теперь ваше приложение использует настоящую аутентификацию!

- ✅ Данные не теряются
- ✅ Безопасная аутентификация
- ✅ Production-ready

**Вопросы?** Проверьте Supabase Dashboard → Authentication → Users

