# 🚀 Шпаргалка: Быстрое переключение моделей

## ⚡ Быстрый старт

### 1. Определите какие модели доступны

```bash
# Откройте OpenAI Playground и проверьте dropdown со списком моделей:
https://platform.openai.com/playground
```

### 2. Откройте конфигурацию

```bash
lib/model-config.ts
```

### 3. Выберите вариант

**Есть GPT-5?** → Используйте ВАРИАНТ 1 (уже активен)  
**Есть O1?** → Переключитесь на ВАРИАНТ 2  
**Только GPT-4o?** → Переключитесь на ВАРИАНТ 3  

---

## 📋 Таблица конфигураций

| Вариант | Primary | Reasoning | Fallback | Контекст | Ответ | Особенности |
|---------|---------|-----------|----------|----------|-------|-------------|
| **1. GPT-5** | `gpt-5` | `gpt-5` | `gpt-4-turbo` | 400K | 32K | Max лимиты, reasoning params |
| **2. O1** | `gpt-4o` | `o1-preview` | `gpt-4-turbo` | 128K | 32K | Developer messages, no temp |
| **3. GPT-4o** | `gpt-4o` | `gpt-4o` | `gpt-4-turbo` | 128K | 16K | Стабильно, доступно всем |

---

## 🔄 Переключение конфигурации

### Текущая конфигурация (по умолчанию): GPT-5

```typescript
// ✅ АКТИВНА
export const MODEL_CONFIGS: Record<ModelName, ModelConfig> = {
  'primary': { name: 'gpt-5', ... },
  ...
};

// ❌ Закомментированы
/*
export const MODEL_CONFIGS: Record<ModelName, ModelConfig> = {
  'primary': { name: 'gpt-4o', ... },
  ...
};
*/
```

---

### → Переключиться на O1 Reasoning

1. **Закомментируйте** текущую конфигурацию:
```typescript
/*
export const MODEL_CONFIGS: Record<ModelName, ModelConfig> = {
  'primary': { name: 'gpt-5', ... },
  ...
};
*/
```

2. **Раскомментируйте** ВАРИАНТ 2:
```typescript
export const MODEL_CONFIGS: Record<ModelName, ModelConfig> = {
  'primary': { name: 'gpt-4o', ... },
  'reasoning': { name: 'o1-preview', ... },
  ...
};
```

3. **Перезапустите:**
```bash
npm run dev
```

---

### → Переключиться на GPT-4o

1. **Закомментируйте** текущую конфигурацию:
```typescript
/*
export const MODEL_CONFIGS: Record<ModelName, ModelConfig> = {
  'primary': { name: 'gpt-5', ... },
  ...
};
*/
```

2. **Раскомментируйте** ВАРИАНТ 3:
```typescript
export const MODEL_CONFIGS: Record<ModelName, ModelConfig> = {
  'primary': { name: 'gpt-4o', ... },
  'reasoning': { name: 'gpt-4o', ... },
  ...
};
```

3. **Перезапустите:**
```bash
npm run dev
```

---

## 🎯 Быстрая диагностика

### Проверка какая модель используется

```bash
# 1. Запустите приложение
npm run dev

# 2. Откройте браузер → F12 → Console

# 3. Сделайте запрос

# 4. Смотрите логи:
[AI Service] Primary model selected: primary
[AI Service] Attempting model: primary (1/3)
AI Response metadata: { modelUsed: 'primary', ... }
```

---

## ⚙️ Кастомизация имен моделей

### Если модель называется по-другому

Просто измените `name` в конфигурации:

```typescript
'primary': {
  name: 'gpt-5-chat-latest', // ← вместо 'gpt-5'
  maxTokens: 32000,
  // ...
},
```

**Возможные варианты имен:**

**GPT-5:**
- `gpt-5`
- `gpt-5-mini`
- `gpt-5-nano`
- `gpt-5-chat-latest`

**O1:**
- `o1-preview`
- `o1-mini`
- `o1-2024-12-17`

**GPT-4o:**
- `gpt-4o`
- `gpt-4o-mini`
- `gpt-4o-2024-08-06`

**GPT-4 Turbo:**
- `gpt-4-turbo`
- `gpt-4-turbo-2024-04-09`
- `gpt-4-turbo-preview`

---

## 🐛 Быстрое решение проблем

### "Model does not exist"
```typescript
// Проблема: неверное имя модели
name: 'gpt-5', // ← проверьте в Playground

// Решение: измените на доступное
name: 'gpt-4o', // ← используйте доступную модель
```

---

### "Invalid 'messages[0].role'" (O1)
```typescript
// Проблема: O1 не поддерживает system
supportsSystemMessages: true, // ← неправильно для O1

// Решение:
supportsSystemMessages: false,
useDeveloperMessage: true,
```

---

### "Invalid parameter: 'temperature'" (O1)
```typescript
// Проблема: O1 не поддерживает temperature
temperature: 0.7, // ← неправильно для O1

// Решение:
temperature: undefined,
```

---

### "Invalid parameter: 'max_tokens'" (O1)
```typescript
// Проблема: O1 использует max_completion_tokens

// Решение: добавьте флаг
useMaxCompletionTokens: true,
```

---

## 📊 Сравнение производительности

### Скорость ответа (примерно)

| Модель | Простой запрос | Сложный запрос | Длинный документ |
|--------|----------------|----------------|------------------|
| GPT-5 | 2-5 сек | 10-20 сек | 30-60 сек |
| O1-preview | 5-10 сек | 20-40 сек | 60-120 сек |
| GPT-4o | 1-3 сек | 5-15 сек | 20-40 сек |
| GPT-4 Turbo | 1-3 сек | 5-15 сек | 20-40 сек |

### Качество анализа

| Модель | Простые задачи | Reasoning | Длинные документы |
|--------|----------------|-----------|-------------------|
| GPT-5 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| O1-preview | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| GPT-4o | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| GPT-4 Turbo | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |

---

## 💰 Примерная стоимость

### Запрос на 10,000 слов:

| Модель | Input | Output | Примерная стоимость |
|--------|-------|--------|---------------------|
| GPT-5 | 5K токенов | 13K токенов | $2-5 |
| O1-preview | 5K токенов | 13K токенов | $3-6 |
| GPT-4o | 5K токенов | 13K токенов | $1-3 |
| GPT-4 Turbo | 5K токенов | 13K токенов | $1-2 |

*Актуальные цены смотрите на https://openai.com/pricing*

---

## ✅ Checklist перед деплоем

- [ ] Выбрана правильная конфигурация
- [ ] Проверены имена моделей в Playground
- [ ] Протестирована на простых запросах
- [ ] Протестирована на сложных запросах
- [ ] Протестирована на длинных документах
- [ ] Проверен fallback (отключите интернет на секунду)
- [ ] Проверены логи в консоли
- [ ] Проверена стоимость в OpenAI Usage dashboard
- [ ] Все работает! 🎉

---

## 🔗 Полезные ссылки

- **OpenAI Playground:** https://platform.openai.com/playground
- **OpenAI Models:** https://platform.openai.com/docs/models
- **OpenAI Pricing:** https://openai.com/pricing
- **Usage Dashboard:** https://platform.openai.com/usage

- **Полная документация:** `MODELS_SETUP.md`
- **AI Models Guide:** `AI_MODELS_GUIDE.md`
- **Quick Start:** `QUICKSTART_AI.md`

---

## 🎯 TL;DR

1. **Проверьте доступ** → OpenAI Playground
2. **Откройте** → `lib/model-config.ts`
3. **Выберите вариант** → Раскомментируйте нужный
4. **Измените имена** → Если нужно
5. **Перезапустите** → `npm run dev`
6. **Проверьте логи** → F12 Console
7. **Done!** ✅

---

Сделано с ❤️ для быстрой настройки AI моделей




