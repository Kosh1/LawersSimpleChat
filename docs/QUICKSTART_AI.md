# Быстрый старт: Система AI с GPT-5

## 🚀 Что изменилось

✅ **Добавлены 3 модели:**
- GPT-5 (основная, приоритет)
- GPT-5-thinking (для сложных задач)
- GPT-4.1 (fallback резерв)

✅ **Автоматический fallback** - если одна модель не работает, система переключается на другую

✅ **Chunking для длинных ответов** - можно генерировать документы на 10,000+ слов

✅ **Умный выбор модели** - система сама выбирает лучшую модель для задачи

---

## 📦 Новые файлы

```
lib/
├── model-config.ts        # Конфигурация GPT-5, GPT-5-thinking, GPT-4.1
├── ai-service.ts          # Логика fallback между моделями
└── response-chunker.ts    # Генерация длинных ответов по частям
```

---

## 🎯 Как это работает

### 1. Простой вопрос → GPT-5
```
Пользователь: "Что такое УК РФ?"
Система: → GPT-5 → быстрый ответ
```

### 2. Сложная задача → GPT-5-thinking
```
Пользователь: "Проанализируй стратегию защиты по делу"
Система: → GPT-5-thinking → глубокий анализ
```

### 3. Длинный документ → GPT-5-thinking + chunking
```
Пользователь: "Подготовь жалобу. Не менее 10000 слов"
Система: → GPT-5-thinking → генерация по частям → объединение
```

### 4. Ошибка → автоматический fallback
```
GPT-5 (rate limit) → GPT-4.1 → ответ получен
```

---

## 🔧 Запуск

### 1. Убедитесь что есть API ключ

```bash
# .env или .env.local
OPENAI_API_KEY=sk-...
```

### 2. Установите зависимости (если нужно)

```bash
npm install
```

### 3. Запустите dev сервер

```bash
npm run dev
```

### 4. Откройте приложение

```
http://localhost:3000
```

---

## ✅ Проверка работы

### Тест 1: Простой вопрос
```
"Что такое апелляция?"
```
**Ожидается:** Быстрый ответ от GPT-5

### Тест 2: Сложная задача  
```
"Проанализируй риски и подготовь стратегию защиты"
```
**Ожидается:** Детальный анализ от GPT-5-thinking

### Тест 3: Длинный документ
```
"Подготовь дополнения к апелляционной жалобе. Не менее 10000 слов"
```
**Ожидается:** 
- Модель: GPT-5-thinking
- Chunking: 2-4 части
- Время: 30-60 секунд
- Результат: полный документ

### Проверка метаданных

Откройте консоль браузера (F12) → Console:

```javascript
[AI Response] {
  model: 'gpt-5-thinking',
  fallback: false,
  chunks: 3,
  tokens: 87532,
  time: '45230ms'
}
```

---

## 🔍 Мониторинг

### В консоли браузера (F12):
```
✨ Ответ был сгенерирован в 3 частей для обеспечения полноты
```

### В консоли сервера (терминал):
```
[AI Service] Primary model selected: gpt-5-thinking
[Chunker] Final result: 3 chunks, 339690 chars, 87532 tokens
```

---

## ⚙️ Настройка

### Изменить приоритет модели

`lib/model-config.ts`:
```typescript
export const MODEL_CONFIGS = {
  'gpt-5': {
    priority: 1, // Меньше = выше приоритет
  }
}
```

### Изменить ключевые слова для GPT-5-thinking

`lib/model-config.ts`:
```typescript
export const REASONING_KEYWORDS = [
  'проанализируй',
  'стратегия',
  // добавьте свои...
];
```

### Изменить лимиты токенов

`lib/model-config.ts`:
```typescript
'gpt-5': {
  maxTokens: 32000,      // Токенов на ответ
  contextWindow: 400000, // Токенов контекст
}
```

---

## 🐛 Troubleshooting

### Проблема: "Empty response from AI Service"
**Решение:** 
1. Проверьте API ключ в `.env`
2. Проверьте логи в терминале
3. Убедитесь что модель доступна

### Проблема: Ответ обрезается
**Решение:**
- Chunking должен сработать автоматически
- Проверьте логи: `[Chunker] Response was truncated`
- Если нет - увеличьте `MAX_CONTINUATION_ATTEMPTS`

### Проблема: Медленная генерация
**Это нормально для:**
- Длинных документов (10K+ слов)
- GPT-5-thinking (глубокий анализ)
- Ожидается 30-60 секунд

### Проблема: Rate limit errors
**Решение:**
- Fallback на GPT-4.1 должен сработать автоматически
- Проверьте логи: `fallbackOccurred: true`
- Проверьте квоту OpenAI API

---

## 📚 Дополнительно

**Полная документация:** `AI_MODELS_GUIDE.md`

**Архитектура системы:** См. диаграммы в гиде

**API Reference:** 
- `lib/ai-service.ts` - основной сервис
- `lib/model-config.ts` - конфигурация
- `lib/response-chunker.ts` - chunking

---

## 📊 Статистика (пример)

**Запрос на 10,000 слов:**
- Модель: GPT-5-thinking
- Chunks: 3
- Токены: ~87,000
- Время: ~45 секунд
- Стоимость: ~$2-3 (зависит от тарифа OpenAI)

---

## 🎉 Готово!

Система готова к использованию. Попробуйте запросить:

```
"Подготовь подробные дополнения к апелляционной жалобе. Не менее 10000 слов"
```

И наблюдайте как система:
1. ✅ Выбирает GPT-5-thinking
2. ✅ Генерирует в 3-4 частях
3. ✅ Объединяет в полный документ
4. ✅ Возвращает результат

**Вопросы?** Смотрите `AI_MODELS_GUIDE.md`

