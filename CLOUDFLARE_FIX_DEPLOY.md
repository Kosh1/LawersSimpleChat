# 🔧 Исправление проблемы с деплоем на Cloudflare Pages

## ✅ Хорошие новости
Сборка проходит успешно! Проблема только в настройках деплоя.

## ❌ Проблема
В настройках Cloudflare Pages установлена неправильная команда деплоя: `npx wrangler deploy`

Эта команда предназначена для **Workers**, а не для **Pages**.

## ✅ Решение

### Вариант 1: Убрать команду деплоя (РЕКОМЕНДУЕТСЯ)

1. Откройте **Cloudflare Dashboard** → ваш проект **Pages**
2. Перейдите в **Settings** → **Builds & deployments**
3. Найдите раздел **"Deploy command"** или **"Post-build command"**
4. **Удалите** команду `npx wrangler deploy` или оставьте поле **пустым**
5. Сохраните изменения

Cloudflare Pages автоматически задеплоит результат сборки из директории `.vercel/output/static`.

### Вариант 2: Исправить Build command

Если команда деплоя обязательна, убедитесь, что **Build command** правильный:

**Текущий (неправильный):**
```
npm run build
```

**Правильный:**
```
npm run build && npx @cloudflare/next-on-pages@latest
```

Это создаст правильную структуру в `.vercel/output/static`, которую Cloudflare Pages автоматически задеплоит.

### Вариант 3: Использовать правильную команду деплоя (если поле обязательно)

Если поле "Deploy command" нельзя оставить пустым, используйте:

```
npx wrangler pages deploy .vercel/output/static --project-name=lawer-chat-bot
```

Но это не рекомендуется - лучше оставить пустым для автоматического деплоя.

## 📋 Проверка настроек

Убедитесь, что настройки следующие:

- ✅ **Build command**: `npm run build && npx @cloudflare/next-on-pages@latest`
- ✅ **Build output directory**: `.vercel/output/static`
- ✅ **Deploy command**: **ПУСТО** (или удалено)
- ✅ **Node.js version**: `18` или `20`

## 🎯 После исправления

После того, как вы уберете или исправите команду деплоя:
1. Cloudflare автоматически задеплоит результат сборки
2. Ваш сайт будет доступен на домене Cloudflare Pages
3. Все функции будут работать (при условии, что переменные окружения установлены)

## 📝 Примечание

Если вы не видите опцию "Deploy command" в настройках:
- Возможно, она находится в другом разделе
- Или Cloudflare Pages использует автоматический деплой по умолчанию
- В этом случае просто убедитесь, что Build command правильный

