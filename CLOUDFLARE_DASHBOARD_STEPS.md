# 🎯 Пошаговая инструкция: Исправление настроек в Cloudflare Dashboard

## ⚠️ ВАЖНО: Это нужно сделать ВРУЧНУЮ в Cloudflare Dashboard!

Не в коде, а в веб-интерфейсе Cloudflare.

## 📝 Шаг 1: Откройте настройки проекта

1. Войдите в [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. Слева выберите **Workers & Pages**
3. Найдите ваш проект **Pages** и кликните на него
4. Перейдите во вкладку **Settings** (вверху страницы)

## 📝 Шаг 2: Измените Build Command

1. Прокрутите до раздела **Builds & deployments**
2. Найдите поле **"Build command"**
3. **УДАЛИТЕ** текущее значение: `npm run build`
4. **ВВЕДИТЕ** новое значение: `npm run build && npx @cloudflare/next-on-pages@latest`
5. Нажмите **Save**

## 📝 Шаг 3: Удалите Deploy Command

1. В том же разделе **Builds & deployments**
2. Найдите поле **"Deploy command"** (или "Post-build command")
3. **УДАЛИТЕ** полностью команду: `npx wrangler deploy`
4. **ОСТАВЬТЕ ПОЛЕ ПУСТЫМ**
5. Нажмите **Save**

Если поле нельзя оставить пустым, введите:
```
echo "Deployment handled automatically"
```

## 📝 Шаг 4: Проверьте Build Output Directory

1. В разделе **Builds & deployments**
2. Найдите поле **"Build output directory"**
3. Убедитесь, что там указано: `.vercel/output/static`
4. Если нет - исправьте и нажмите **Save**

## ✅ Финальная проверка настроек

После всех изменений настройки должны быть такими:

```
Framework preset: Next.js (или None)
Build command: npm run build && npx @cloudflare/next-on-pages@latest
Build output directory: .vercel/output/static
Root directory: / (пусто)
Node.js version: 18 или 20
Deploy command: (ПУСТО - удалено)
```

## 🚀 Шаг 5: Запустите новый деплой

1. После сохранения настроек Cloudflare может автоматически запустить новый деплой
2. Или перейдите во вкладку **Deployments**
3. Найдите последний неудачный деплой
4. Нажмите на три точки (⋮) рядом с ним
5. Выберите **Retry deployment**

## ⏱️ Ожидайте результата

Новый деплой должен пройти успешно:
- ✅ Сборка пройдет (как и раньше)
- ✅ Деплой также пройдет успешно (теперь без ошибки wrangler deploy)

## 📸 Скриншоты (если нужно)

Если вы не можете найти нужные поля:
- Они находятся в разделе **Settings** → **Builds & deployments**
- Это стандартный раздел для всех проектов Cloudflare Pages

## ❓ Если не получается

1. Убедитесь, что вы редактируете проект типа **Pages**, а не **Workers**
2. Проверьте, что у вас есть права на изменение настроек проекта
3. Попробуйте обновить страницу и проверить настройки снова

